/* Brand mark paths the story draws inline (Shopify, n8n, HubSpot). They are
   the official simple-icons paths, the same source as
   components/brand-marks.tsx, named here so the views stay readable. */

import { siHubspot, siN8n, siShopify } from 'simple-icons';

export const D_SHOPIFY = siShopify.path;
export const D_N8N = siN8n.path;
export const D_HUBSPOT = siHubspot.path;
