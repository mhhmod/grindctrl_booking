You are working in the grindctrl_booking repository on the Next.js app in apps/web-next. The owner has approved a new design, version 15, for three public pages: the landing page at the root, the public try-on page at /try-on and the pricing page at /pricing. Your job is to build them for real in this app. They must work in English and Arabic, left to right and right to left, look right on every phone and desktop width, and be wired to the backend that already exists here, so that everything a visitor can click actually does something.

Everything you need is in apps/web-next/docs/handoff/site-v15. Start by reading its README.md, then the six numbered notes in order, 01-system.md through 06-qa.md, and then the repo's own AGENTS.md, apps/web-next/DESIGN.md, apps/web-next/PRODUCT.md and docs/golden-landing-standard.md. The handoff folder also holds reference screenshots of every state in design/screens, the prototype's source for exact sizes, timings and behaviour in design/prototype, every image the pages use in assets/images with a manifest, the icon set and brand marks in assets/icons, every string in English with its approved Arabic in copy, and the design values mapped to the existing tokens in tokens.json. Treat the screenshots as the visual target and the prototype source as the reference for behaviour. The prototype's canvas runtime tags, its localStorage language key, its width-based layout switch and its CSS zoom are prototype mechanics, and none of them should reach this codebase.

Pushing to main deploys to production, so everything goes through pull requests. Start with the try-on model bug described in 05-backend.md, on its own branch from main named fix/try-on-model-tier, with its tests, in its own pull request. It is breaking every paid storefront try-on today and has nothing to do with the redesign. Then create the redesign branch from main, named feat/site-v15, and build the shared pieces from 01-system.md. That means the new tokens added to globals.css, the icon set, the floating header with Sign in visible on desktop and as the first item of the phone menu, the footer that keeps the analytics consent control and theme toggle, the language switch that keeps using the gc-locale cookie and also updates the html lang and dir immediately, the store password chip and the background wiring. Then rebuild the pricing page on the live catalog and currency logic as 04-pricing.md describes. Then build the new try-on page on the real try-on API as 03-try-on.md describes, without changing the embedded storefront try-on that shares components with the current page. Finally build the landing story as 02-landing.md describes, porting the prototype's director faithfully and adding the production behaviour the note lists, such as reduced motion, inert covered sheets and deep links.

Follow the repo's rules as you go. Take colours only from the tokens, use logical properties so Arabic mirrors naturally, keep the landing root wrapper and the max-w-7xl container the guard tests expect, check components/ui before adding any primitive, never run the Launch UI shadcn command, never invent copy or claims, and route every claim through lib/product-truth. Don't hard code prices. Where the design deliberately changes something a test pins down, such as the landing heading or the old how section in the RTL spec, update the test in the same change and explain why in the pull request. Never loosen a check just to make it pass. If a string you need has no approved Arabic in the copy folder or the repo, stop and list it rather than translating it yourself.

When the pages are built, work through 06-qa.md. That means lint, typecheck, unit tests, the build, the golden Playwright specs, the mobile overflow spec, the new specs it asks for, the manual checks at every listed width in both languages and both themes, a real browser pass on an iPhone and an Android phone, and a real try-on on your machine with TRYON_MODE set to live, using keys the owner gives you, because there is no staging environment. Then open a pull request against main that says what you built, which tests changed and why, which environment variables the owner has to set, and the known gaps listed in 05-backend.md. List anything you could only verify with mocks, and the steps the owner must take after deploying, such as pressing Generate on the demo store. Also ask the owner the two open questions in README.md: whether the managed setup claim may ship, and what to do about the pricing answer on running out of credits. Attach before and after screenshots at 390 and 1440 in English and Arabic. Say plainly what you verified in a real browser and what you did not.
