# grindctrl-tryon (extension-only)

Shopify theme app extension for the GrindCTRL try-on block.
The app backend lives in `apps/web-next` (embedded admin at /shopify/app).

Deploy the block: `npx shopify app deploy -f`
