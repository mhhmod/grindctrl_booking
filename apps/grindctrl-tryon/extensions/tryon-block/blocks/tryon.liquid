{% comment %}
  GrindCTRL Try-On app block: renders a button on the product page that
  expands the try-on experience inline (iframe to the GrindCTRL embed).
{% endcomment %}
<div
  class="gc-tryon-root"
  data-embed-base="{{ block.settings.embed_base }}"
  data-product="{{ product.handle }}"
  data-shop="{{ shop.permanent_domain }}"
  data-locale="{{ request.locale.iso_code }}"
  data-theme="{{ block.settings.widget_theme }}"
  {{ block.shopify_attributes }}
>
  <button type="button" class="gc-tryon-btn">{{ block.settings.button_label }}</button>
</div>
<link rel="stylesheet" href="{{ 'tryon.css' | asset_url }}">
<script src="{{ 'tryon.js' | asset_url }}" defer></script>

{% schema %}
{
  "name": "GrindCTRL Try-On",
  "target": "section",
  "settings": [
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label",
      "default": "Try it on with AI"
    },
    {
      "type": "select",
      "id": "widget_theme",
      "label": "Widget theme",
      "options": [
        { "value": "", "label": "From GrindCTRL dashboard" },
        { "value": "light", "label": "Light" },
        { "value": "dark", "label": "Dark" }
      ],
      "default": ""
    },
    {
      "type": "text",
      "id": "embed_base",
      "label": "GrindCTRL base URL",
      "default": "https://grindctrl.cloud",
      "info": "Leave as-is unless GrindCTRL tells you otherwise."
    }
  ]
}
{% endschema %}
